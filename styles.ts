import { StyleSheet } from 'react-native';

export const globalStyles = StyleSheet.create({
    appCenter: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
    colorText: {
        color: '#212121',
    },
});
